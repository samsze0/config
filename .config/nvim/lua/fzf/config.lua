return {
  debug = false,
  fzf_selection_cache = "~/.cache/fzf_selection",
  bat_default_opts = "--color=always",
  delta_default_opts = "",
  rg_default_opts = "--color=always --smart-case --no-ignore --hidden",
}
